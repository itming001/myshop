spring:
  application:
    name: myshop-oauth2
  jackson:
    time-zone: GMT+8
    date-format: yyyy-MM-dd HH:mm:ss
  cloud:
    nacos:
      discovery:
        server-addr: 127.0.0.1:8848
server:
  port: 9001
management:
  endpoints:
    web:
      exposure:
        include: "*"